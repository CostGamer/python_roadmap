# Python
1) Что такое компилируемых и интерпретируемых языки? В чем их отличие?
2) Плюсы и минусы `Python`
3) Какие задачи решает `Python`? 


# Ввод - вывод
## print()
4) Как работает `print()`?
5) Что такон аргумент?

## input()
6) Как работает `input()`? Что принимает `input()`?
7) Что такое переменная? Как можно и принято называть переменную?
8) Что такое комментарий и как его делать?

9) Несколько переменных в `print()`
10) Переменные в `input()`
11) Параметры `sep` & `end` в `print()`

## PEP8
12) Что такое `PEP8`?
13) Обшие рекомендации

## Целочисленные операции
14) `int()`, `int(input())`
15) Математические операции
16) Операторы: (), **, //, %


# Условный оператор
17) `if-else`
18) Что такое табуляция?
19) Какие есть операторы сравнения? Можно еще посмотреть цепочки сравнения и транзитивность
20) Логические операции: `and`, `or`, `not`
21) Комбинирование и приоритизвация логических операций
22) Вложенные условия
23) Каскадные условия. Что такое `elif`


# Типы данных 1
24) Что такое тип данных и зачем он нужен?
25) Числовые типы данных. Целый числа и числа с плаващей точкой
26) Функции `min()`, `max()`, `abs()`
27) Функция `round`
28) Строковый тип данных
29) Функции `len()`, `in` для строк!
30) `bool` & `None`


# Строки
31) Индексы
32) Срезы. Срезы с одним/двумя/тремя параметрами `[x:y:z]`
33) Отрицательные срезы
34) Методы строк (можно посмотреть все, но укажу, который точно надо знать):
- `capitalize()`
- `title()`
- `lower()`
- `upper()`
- `startswith()`
- `strip()`
- `isalpha()`
- `isdigit()`
- `islower()`
- `isupper()`
35) Конкатенация и другие математические операции
36) Форматирование:
- `format()`
- f-строки
37) Если интересно можно посмотреть, как строки хранятся в памяти компьютера. Команды `ord()`, `chr`
38) Если интересно, можно посмотреть про кодировки


# Циклы
## For loop
39) Как написать for loop в `Python`?
40) Функция `range`. `Range` с несколькими параметрами и отрицательным шагом генерации
41) `_` в цикле for
42) Концепция `флага` (сигнала)

## While loop
43) Как написать while loop в `Python`?
44) Бесконечные циклы
45) Операторы: `break`, `continue`, `else`. Кейсы приминения

## Вложенные циклы
46) Что такое вложенные циклы?
47) Как работают операторы `break`, `continue`, `else` во вложенных циклах


# Типы данных 2
## Списки
48) Что такое список? На что он похож в других языках (доп знание). Функция `list()`. Изменяемость списков. 
49) Что такое коллекции и последовательности?
50) Функции `len()`, `in` для списков
51) Индексы
52) Slices. Посмотреть различные вариации
53) Функции `sum()`, `min()`, `max()`
54) Конкатенация и другие математические операции
55) Методы списков (можно посмотреть все, но укажу, который точно надо знать):
- `append()`
- `extend()`
- оператор `del`
- `remove()`
- `pop()`
- `reverse()`
- `copy()`
- `clear()`
- `sort()`
56) Уметь перебирать списки через for
57) Распаковка списков
58) Методы `split()`, `join()`
59) Списочные выражение (но лучше говорите list comprehensions). Посмотреть, как писать вместе с условием
60) Вложенные списки. Если есть время, можно попробовать матрицы
61) Сортировка: `sorted()`. Можно вернуться позже


# Функции 1 
62) Что такое функциональное программирование?
63) Что такое функция? Как объявить функцию в Python?
64) Функции с и без параметров
65) Разница аргумента и параметра
66) Локальные и глобальные переменные. Область видимости. Правило `LEGB`
67) Разные виды `return`


# Типы данных 3
## Кортежи
68) Что такое кортежи?
69) В чем их плюсы и минусы? Их свойства. Отличие от списков
70) Функции `len()`, `in` для кортежей и другие по типу `sum()`
71) Методы кортежей:
- `index()`
- `count()`
- `join()`
72) Распаковка кортежей

## Множества
73) Что такое множества? Свойства множеств
74) Функции `len()`, `in` для множеств и другие по типу `sum()`
75) Методы множеств (можно посмотреть все, но укажу, который точно надо знать):
- `add()`
- `remove()`
- `discard()`
- `pop()`
- `clear()`
- методы и операторы для операций между нмножествами
- `issubset()`
- `issuperset()`
76) Генераторы множеств или `set comprehensions`
77) Можно посмотреть, что такое `frozenset`

## Словари
78) Что такое словари? Свойства словарей
79) Что может быть ключом словаря, а что нет?
80) Функции `len()`, `in` для словарей и другие по типу `sum()`
81) Методы словарей (можно посмотреть все, но укажу, который точно надо знать):
- получение элемента из словаря
- удаления элемента из слвоваря
- `keys()`
- `values()`
- `items()`
- `get()`
- `update()`
- `setdefault()`
- `pop()`
- `clear()`
- `copy()`
82) Уметь перебирать словари через for
83) Вложенные словари
84) Генераторы словарей или `dict comprehensions`


# Управление памятью
85) Устройство памяти в общем
86) Устройства памяти в Python
87) Перемнные в Python. Как работают ссылки в Python
88) Изменяемые и неизменяемые типы данных
89) `copy()` & `deepcopy()`
90) Garbage collector. Слабые и сильные ссылки


# Функции 2
91) Встроенные функции `type()`, `sorted()`, `reversed()`, `isinstance()`, `callable()`, `hasattr()`, `hash()` и другие
92) Позиционные и именованные аргументы
93) Аргументы по умолчанию
94) Функции высшего порядка
95) `lambda` функции
96) `map()`, `filter()`, `reduce()`. Кейсы их применения
97) `any()`, `all()`, `zip()`, `enumerate()`


# Работа с файлами
98) Потоковый ввод и вывод данных

## TXT
99) Что такое контекстный менеджер?
100) Как читать и записывать в файл?
101) Какие есть функции и методы для работы с TXT в Python?

## JSON
102) Что такое JSON? Для чего он нужен?
103) Какие есть функции и методы для работы с JSON в Python?
104) Как происходит конвертация типов данных при сериализации?

## CSV
105) Что такое CSV? Для чего он нужен?
106) Какие есть функции и методы для работы с CSV в Python?


# Работа с датой и временем
107) Посмотреть модуль `datetime` и различные типы данных в нем: `date`, `time`, etc.
108) На дате можно изучить `repr()` и посмотреть разницу с `str()`
109) `strftime()`, `strptime()`, `isoformat()`, `fromisoformat()`, `combine()`, `now()`
110) Что такое начало эпохи?
111) Что такое `timedelta` и зачем оно нужно?
112) Посмотреть, что такое временные зоны и как с ними работать


# Обработка исключений
113) Типы ошибок. Основные исключения в Python и их иерархия
114) `try-except`
115) `else` & `finally` в `try-except`
116) Оператор `raise`
117) Кастомные ошибки


# Рекурсия
118) Что такое рекурсия?
119) Какие задачи решает рекурсия?


# Функции 3
120) Вложенные функции
121) Замыкание. Зачем оно нужно?


# Декораторы
122) Что такое декоратор? Как написать свой декоратор?
123) Есть ли что-то похожее в других языках?
124) Атрибуты `__name__` & `__doc__`
125) `@functools.wraps` - что такое и зачем?
126) Параметризованный декоратор

# Итераторы
127) Что такое итераторы? Зачем они нужны? В чем их профит?
128) Генераторы
129) В чем разница генераторов от итераторов?
130) Когда использовать и кейсы
131) Повторить, что такое `range`

# Type hints
132) Что такое и зачем нужны?
133) Что такое динамическая типизация?
134) модуль `typing`
135) Если есть желание, посмотреть, как было на более старых версиях


# Extra Python
`P.S: only for flexing on the job interview`
136) Модуль `itertools`
137) Модуль `functools`
138) Регулярные выражения. Модуль `re`


# Cache
139) Что такое кэш?
140) `lru_cache` и мемоизация
141) Методы кэширования
142) Что такое хэш?
143) Разница кэша и хэша


# Типы данных 4
`P.S: only for flexing on the job interview`
144) `Decimal` что такое и для чего нужен?
145) `namedtuple` что такое и для чего нужен?
146) `defaultdict` что такое и для чего нужен?
147) `OrderedDict` что такое и для чего нужен?
148) `ChainMap` что такое и для чего нужен?
149) `deque` что такое и для чего нужен?
150) Можно еше поизучать модуль `collections`


# Принципы ООП
151) Парадигмы программирования. Что такое объект в Python?
152) Преимущества ООП и недостатки
153) Что такое класс и объект?
154) 4 принципа ООП
- абстракция
- инкапсуляция
- наследование
- полиморфизм


# Атрибуты и методы
155) Какие кейсы есть вообще, и какие соглашение на использования в Python (camelCase, snake_case, etc)
156) `dir()`
156) Создание классов и объектов в Python
157) Атрибуты класса и объекта (без `__init__`)
158) Атрибут `__dict__`
159) `getattr()`, `setattr()`, `delattr()`, `hasattr()`
160) Что такон метод и как его делать?


# Методы экземпляра класса
161) Метод `__init__`
162) Параметр `self` что такое и зачем?


# Доступ к атрибутам
163) Как сокрытие данных реализовано на Python?
164) Соглашение между разработчиками
165) Что такон `геттеры`, `сеттеры`, `делитеры` и зачем они нужны?
166) Что такое свойство (property)
167) `@property`. Обязательно понять, как работает
168) Декораторы `@classmethod` и `@staticmethod`. Что такое `cls`
169) Если есть время, посмотреть `@singledispatchmethod`. Перегрузка методов


# Виртуальное окружение
## venv
## poetry
## 


# Логирование
